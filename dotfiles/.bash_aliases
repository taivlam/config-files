# My most commonly used pair of Git commands, when updating AUR/MPR packages
alias git_diff-HEAD_show='git diff HEAD && git show'

# Shortcut for `ls -al`
alias la='ls -al'

# Display disk usage of all immediate subdirectories and files
alias sorted_du='du -hs * | sort -h'

