#
# ~/.bashrc
#

# Initialize use of separate Bash alias file.
# Uses terse Bash version of if-then statements.
# Modularization prevents possibly messing up `~/.bashrc`:
[ -f ~/.bash_aliases ] && . ~/.bash_aliases
